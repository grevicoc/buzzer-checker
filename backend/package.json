{
  "name": "buzzer-checker-backend",
  "version": "1.0.0",
  "description": "open-source project (soon) for checking is someone in twitter is a buzzer or not (for now indonesian only)",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "grevi",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^4.7.2"
  }
}
